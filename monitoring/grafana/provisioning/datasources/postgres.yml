apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    url: postgres:5432
    database: helpdesk
    user: helpdesk
    secureJsonData:
      password: helpdesk123
    jsonData:
      sslmode: disable
      postgresVersion: 1600